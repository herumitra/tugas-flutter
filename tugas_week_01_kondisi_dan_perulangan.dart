void main() {
  // Perulangan dari 1 sampai 20
  for (int i = 1; i <= 20; i++) {
    // Memeriksa apakah bilangan genap
    if (i % 2 == 0) {
      // Menampilkan bilangan genap
      print(i);
    }
  }
}
